class Solution {
public:
    int numberOfEmployeesWhoMetTarget(vector<int>& hours, int target) {
        int i = 0;
        int n = hours.size();
        int cnt = 0;
        for(i;i<n;i++){
            if(hours[i] >= target){
                cnt++;
            }
        }
        return cnt;
    }
};
